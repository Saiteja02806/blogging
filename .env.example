NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_TOKEN=

# Supabase (likes + comments — Settings → API in dashboard)
# Prefer these on Vercel so API routes see credentials without rebuild issues:
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Optional (browser); API routes also read these as fallback:
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
